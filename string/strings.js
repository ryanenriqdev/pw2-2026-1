const nome = "Francisco";

console.log(`Nome: ${nome}, qtd de caracteres: ${nome.length}`);

const nomeObject = new String("Francisco");

const nomeStr = String("Francisco");

const object = { };
object.nome = 3;

// console.log( typeof nome );
// console.log( typeof nomeStr );
// console.log( typeof nomeObject );

console.log( typeof Float32Array(10) );

console.log( parseInt("3") );
// parseFloat -> recebe string => number da string recebida